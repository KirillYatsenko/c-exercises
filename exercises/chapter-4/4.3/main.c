#include <stdio.h>
#include <stdlib.h>
#include "polish-calculator.h"
#include "stack.h"

int main()
{
    /* push(10.0);
    push(3.0);

    swaphead();
    swaphead();

    clear();
    printf("\n peak = %f", peak()); */

    calculate();

    return 0;
}
